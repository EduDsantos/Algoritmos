algoritmo "Contador usando 'Para'"
var
    C,V, cont:inteiro
inicio
      escreval("digite um valor ")
      leia(v)
      limpatela
      se( v%2= 1)entao
      v<- v - 1
      fimse
      para cont<- v ate 0 passo -2 faca
           escreval(cont)
      fimpara

fimalgoritmo